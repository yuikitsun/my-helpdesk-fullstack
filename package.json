{
  "name": "helpdesk-full-stack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix 112-devices\" \"npm run dev --prefix helpdesk-backend\"",
    "dev:frontend": "npm run dev --prefix 112-devices",
    "dev:backend": "npm run dev --prefix helpdesk-backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}